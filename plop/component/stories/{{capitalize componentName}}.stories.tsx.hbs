import { Meta } from '@storybook/react';

import { {{capitalize componentName}} } from '@shared-ui/{{dashCase componentName}}';

export default {
  title: "Components/{{capitalize componentName}}",
  component: {{capitalize componentName}},
  argsTypes: {
    // add your args types here
  }
} as Meta<object>;